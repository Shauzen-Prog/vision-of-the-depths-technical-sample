/// <summary>
/// Evento disparado cuando el jugador entra en un closeup (pantalla, consola, etc.).
/// </summary>
public readonly struct CloseupStartedEvent { }

/// <summary>
/// Evento disparado cuando el jugador sale de un closeup.
/// </summary>
public readonly struct CloseupEndedEvent { }
