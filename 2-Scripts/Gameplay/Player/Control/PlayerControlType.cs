/// <summary>
/// Tipos de control del jugador que pueden ser habilitados o bloqueados
/// por distintos sistemas (closeup, diálogo, pausa, cutscenes, etc.).
/// </summary>
public enum PlayerControlType
{
    Movement,
    Look,
    Interaction,
    Pause
}
